//Backtracking
#include <iostream>

using namespace std;

int main()
{
    //ios::sync_with
    int n, Case = 0;
    while(cin >> n && n)
    {
        //if(!n)  break;
        if(Case++) cout << endl;

        long long arr[n];
        for(int i = 0;i < n;i++)
        {
            cin >> arr[i];
        }

    }
    return 0;
}
